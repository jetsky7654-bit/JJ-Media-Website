'use client'
import { FormEvent, useState } from 'react'
export function ContactForm(){
 const [status,setStatus]=useState<'idle'|'sending'|'success'|'error'>('idle')
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('sending');const form=new FormData(e.currentTarget);const data:Record<string,unknown>=Object.fromEntries(form.entries());data.consent=true;try{const res=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!res.ok)throw new Error();setStatus('success');e.currentTarget.reset()}catch{setStatus('error')}}
 return <form className="contact-form" onSubmit={submit}><div className="form-row"><div><label htmlFor="name">Name *</label><input id="name" name="name" required/></div><div><label htmlFor="email">E-Mail *</label><input id="email" name="email" type="email" required/></div></div><div className="form-row"><div><label htmlFor="company">Unternehmen</label><input id="company" name="company"/></div><div><label htmlFor="phone">Telefon</label><input id="phone" name="phone"/></div></div><div><label htmlFor="budget">Projektbudget</label><select id="budget" name="budget"><option value="">Bitte wählen</option><option>unter 2.000 €</option><option>2.000–5.000 €</option><option>5.000–10.000 €</option><option>über 10.000 €</option></select></div><div><label htmlFor="message">Wobei können wir helfen? *</label><textarea id="message" name="message" required/></div><label><input type="checkbox" required style={{width:'auto',marginRight:8}}/> Ich stimme der Verarbeitung meiner Angaben zur Bearbeitung der Anfrage zu.</label><button className="btn btn-coral" disabled={status==='sending'}>{status==='sending'?'Wird gesendet …':'Projekt anfragen ↗'}</button>{status==='success'&&<div className="form-status">Danke! Deine Anfrage wurde gespeichert.</div>}{status==='error'&&<div className="form-status">Die Anfrage konnte nicht gesendet werden. Bitte schreibe direkt an service@jj-media.info.</div>}</form>
}
